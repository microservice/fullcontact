commands:
  person:
    help: Search for a person
    kwargs:
      - name: email
        type: string
      - name: twitter
        type: string
      - name: phone
        type: string
  company:
    help: Search for a company
    args:
      - name: domain
        type: string
        required: true

env:
  API_KEY:
  USER_AGENT:
    default: Asyncy
